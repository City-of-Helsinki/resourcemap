import { defineMessages } from 'react-intl';

const scope = 'components.TouchScreenIndicator';

export default defineMessages({
  touchScreenLabel: {
    id: `${scope}.touchScreenLabel`,
    defaultMessage: 'Kosketusnäyttö',
  },
});
